<scene>
	<!-- Independent sample generator, user-selected samples per pixel -->
	<sampler type="independent">
		<integer name="sampleCount" value="64"/>
	</sampler>

	<integrator type="vol_path" />

	<!-- Render the scene as viewed by a perspective camera -->
	<camera type="perspective">
		<transform name="toWorld">
			<lookat target="0,0,0" origin="0,10,0" up="0,0,1"/>
		</transform>

		<float name="fov" value="70"/>

		<integer name="width" value="1280"/>
		<integer name="height" value="720"/>
	</camera>

	<!-- Earth -->
	<!-- TODO: fix position and scale -->
	<mesh type="sphere">
		<point name="center" value="-15 -20 -25" />
        <float name="radius" value="20" />

        <bsdf type="diffuse">
            <texture type="image_texture" name="albedo">
                <string name="filename" value="earth-texture-flipped.png"/>
				<vector name="shift" value="0.5 0"/>
            </texture>
        </bsdf>

		<transform name="toWorld">
            <rotate axis="1,0,0" angle="90"/>
		</transform>
	</mesh>

	<!-- Background -->
	<mesh type="obj">
		<string name="filename" value="plane.obj"/>

		<bsdf type="diffuse">
            <texture type="image_texture" name="albedo">
                <string name="filename" value="starmap.png"/>
				<vector name="scale" value="30, 30"/>
            </texture>
        </bsdf>

		<transform name="toWorld">
			<scale value="500 500 500"/>
			<translate value="0,-30,0"/>
		</transform>

	</mesh>

	<!-- Lights -->
	<!-- TODO: fix -->
	<emitter type="point">
		<point name="position" value="3,7,10"/>
		<color name="power" value="4000,4000,4000"/>
	</emitter>

	<emitter type="point">
		<point name="position" value="-10,-10,20"/>
		<color name="power" value="4000,4000,4000"/>
	</emitter>

	<emitter type="directional">
		<vector name="direction" value="-0.1,-1.0,-0.2"/>
		<color name="power" value="1000,1000,1000"/>
	</emitter>

</scene>
